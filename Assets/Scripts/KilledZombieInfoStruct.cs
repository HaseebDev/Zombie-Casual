using System;
using UnityEngine;

public struct KilledZombieInfoStruct
{
    public GameMode gameMode;
    public bool isAttacked;
    public int killReward;
    public bool isBoss;
    public Vector3 worldPosition;
    public string killerID;
}
